script(src='/javascripts/less.js', type='text/javascript')
script(type='text/javascript').
	var _gaq = _gaq || [];
	_gaq.push(['_setAccount', 'UA-36112904-2']);
	_gaq.push(['_trackPageview']);

	(function() {
		var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
		ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
		var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
	})();
script(type='text/javascript').
	var testData = new Firebase("https://scorching-fire-2712.firebaseio.com/");

	$('#user').keypress(function (e) {
		if (e.keyCode == 13) {
			var balls = $('#user').val();
			testData.push({ testes: balls });
		}
	});

	testData.on("value", function(data) {
		var message = data.val();
		changeTitle(message.testes);
	});

	function changeTitle(title) {
		$('#title').empty();
		$('#title').append(title);
	};
